# React Hook Implementation

Read the given PHP Route and generate the corresponding client-side API call using React Hooks and `fetch`. The back-end API uses snake_case for its property names while the front-end uses camelCase, so you will have to map any snake_case property that the front-end receives to camelCase. The React Hook should live in the Domain that corresponds to the closest domain on the back-end.
